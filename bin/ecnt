#!/bin/bash
#
# Opens given file in a new Emacs tab.
# Assumes Emacs is already running.

if pgrep -f Emacs > /dev/null
then
    emacsclient --eval '(tab-bar-new-tab)' &>/dev/null
    emacsclient $@ &>/dev/null &
    disown
else
    open -a /Applications/Emacs.app $@
fi
